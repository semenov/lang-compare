[env]
# The bundled SQLite otherwise keeps global memory statistics behind a mutex taken on every
# allocation, which serializes all threads (better-sqlite3 builds with the same flag).
LIBSQLITE3_FLAGS = "-DSQLITE_DEFAULT_MEMSTATUS=0"
